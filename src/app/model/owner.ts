export class Owner {
  // id: number;
  // firstName: string;
  // lastName: string;
  // phone: string;
  // note: string;
  //
  constructor(
    public id?: number,
    public firstName?: string,
    public lastName?: string,
    public phone?: string,
    public note?: string) {
  }
}
